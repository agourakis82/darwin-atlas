# U0 control-ledger fixture

This tiny synthetic rehydrated-package layout exists only to exercise exact
ledger binding.  The test generates its ledger from these immutable bytes and
then proves byte tampering, assembly/path cross-binding, false biological
claims, symlinks, and path escape all fail closed.
